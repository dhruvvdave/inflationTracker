# Database Connection String
# Choose one of the following options:

# Option 1: Docker (Easiest for local development)
# Run: docker-compose up -d
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/inflationlens"

# Option 2: Local PostgreSQL
# Requires: PostgreSQL installed and running
# Create database: createdb inflationlens
# DATABASE_URL="postgresql://postgres:yourpassword@localhost:5432/inflationlens"

# Option 3: Supabase (Free cloud database)
# Get from: Supabase Dashboard → Settings → Database → Connection String (Transaction mode)
# DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@[HOST]:[PORT]/postgres?sslmode=require"

DATABASE_URL="postgresql://user:password@localhost:5432/inflationlens"

# FRED API Key
# Get your free API key from: https://fred.stlouisfed.org/docs/api/api_key.html
# This is required to fetch CPI data
FRED_API_KEY="YOUR_FRED_KEY"

# Application URL
# For local development, use localhost
# For production, use your deployed URL
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
